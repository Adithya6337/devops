
a=999
b=999
for i in $(seq 1 10)
do
	r=$(( $RANDOM % 900 + 100 ))
	echo $r
	if [ $b -gt $r ]
	then
		a=$b
		b=$r
	elif [ \( $a -gt $r \) -a \( $b -ne $r \) ]
	then
		a=$r
	fi
done
echo

echo "Second smallest $a"
