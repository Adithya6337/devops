
a=999
b=999
for i in $(seq 1 10)
do
	r=$(( $RANDOM % 900 + 100 ))
	echo $r
	if [ $a -gt $r ]
	then
		b=$a
		a=$r
	elif [ \( $b -gt $r \) -a \( $a -ne $r \) ]
	then
		b=$r
	fi
done
echo

echo "Second smallest $b"
